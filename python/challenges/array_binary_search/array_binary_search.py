def BinarySearch(array, search_key):
    index_number = []
    If search_key not in array:
		return(-1)
	Else:
		For i in array:
			If i == search_key
				return index_number
            else if i != search_key:
				index_number += 1